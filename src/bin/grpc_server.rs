// This file is temporarily disabled due to dependency issues
// It will be re-enabled once the handlers and grpc modules are fixed

fn main() {
    println!("gRPC server is temporarily disabled");
}
